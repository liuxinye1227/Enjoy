<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
			http://www.springframework.org/schema/context
			http://www.springframework.org/schema/context/spring-context-3.0.xsd
			http://www.springframework.org/schema/aop
			http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
			http://www.springframework.org/schema/tx
			http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
			http://www.springframework.org/schema/mvc
			http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
			http://www.springframework.org/schema/context
			http://www.springframework.org/schema/context/spring-context-3.0.xsd">
	
	<context:annotation-config/>
	<context:component-scan base-package="com.facishare.pay.business"/>
	
       <!-- 各数据源bean定义 -->
	<bean id="business_dataSource" class="com.alibaba.druid.pool.DruidDataSource" destroy-method="close">
    		<property name="driverClassName" value="${fs.pay.business.mysql.driverClass}" />
			<property name="url" value="${fs.pay.business.mysql.jdbcUrl}" />
			<property name="username" value="${fs.pay.business.mysql.user}" />
			<property name="password" ref="passwordMsgProvider" />
			<property name="initialSize" value="${fs.pay.business.mysql.min.connection.count}" /><!--连接池初始化时创建的连接数。-->
			<property name="maxActive" value="${fs.pay.business.mysql.max.connection.count}" /><!--连接池中保留的最大连接数。Default: 8 -->
			<property name="validationQuery" value="select 1 from dual" />
			<property name="minIdle" value="${fs.pay.business.mysql.min.connection.count}" /><!--最小空闲的连接数,30个。default:0-->
			<property name="testOnBorrow" value="false" />
			<property name="filters" value="stat" />
	</bean>

	
	<bean id="business_sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="business_dataSource" />
		<property name="typeAliasesPackage" value="com.facishare.pay.business.model" />
		<property name="mapperLocations" value="classpath:mapper/*.xml" />
		<property name="configLocation" value="classpath:spring/spring-mybatis.xml" />
	</bean>

	<bean id="business_jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource">
			<ref bean="business_dataSource" />
		</property>
	</bean>
	
	<!-- 事务管理器   -->
	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager"> 
         <property name="dataSource" ref="business_dataSource" /> 
    </bean> 
	<tx:annotation-driven transaction-manager="transactionManager" proxy-target-class="true" /> 

    <bean name="passwordMsgProvider" class="com.facishare.open.common.password.DbPasswordFactoryBean" >
		<property name="cryptWord" value="${fs.pay.business.mysql.password}" />
	</bean>
</beans>